{% extends 'base.html.twig' %}
{% block title %} {{ weapon.type }}{% endblock %}
{% block body %}
    {% block titre %} <h1>{{ weapon.type }}</h1> {% endblock %}

    <div class="row">
        <div class="col-2">
            <img src="{{ asset('images/armes/' ~ weapon.type|lower ~ '.png') }}">
        </div>
        <div class="col">
            <h3> Les caractéristiques</h3>
            <ul>
                <li>Type : {{ weapon.type }}</li>
                <li>Dommages : {{ weapon.dommages }}</li>
                <li>Description : {{ weapon.description }}</li>
            </ul>
        </div>
    </div>
{% endblock %}